import React from "react";
import useStyles from "./styles";

const DisabledProfileCard = (props) => {
    const classes = useStyles();

    return (
        <div>Disabled</div>
    );
};

export default DisabledProfileCard;
