import React from "react";
import useStyles from "./styles";

const AssistantProfileCard = (props) => {
    const classes = useStyles();

    return (
        <div>Asistent</div>
    );
};

export default AssistantProfileCard;
